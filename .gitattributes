# 기본: 텍스트 파일은 전부 LF로 통일 (Windows/Mac/Linux 개발자 혼재 대응)
* text=auto eol=lf

# 예외: Windows 배치 스크립트는 CRLF 유지 (cmd.exe 호환)
*.bat text eol=crlf

# jar는 바이너리 — text=auto 휴리스틱에 맡기지 않고 명시적으로 고정 (Gradle 기본 생성값)
*.jar binary
